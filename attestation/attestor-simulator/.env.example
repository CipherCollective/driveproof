PORT=4000
PROVIDER_ID=1
NETWORK_ID=undeployed
# Exact browser origin allowed to call the simulator; wildcard access is not supported.
ATTTESTOR_ALLOWED_ORIGIN=http://localhost:5173
# Never regenerate on restart — each new key invalidates any on-chain attestor registration tied to the old one.
PROVIDER_SECRET_KEY=
